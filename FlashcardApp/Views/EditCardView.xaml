<UserControl x:Class="FlashcardApp.WPF.Views.EditCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlashcardApp.WPF.Views"
             
             xmlns:viewmodels="clr-namespace:FlashcardApp.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:EditCardViewModel}"
             
             xmlns:controls="clr-namespace:FlashcardApp.WPF.Controls"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <controls:CardInputForm
            SubmitButtonText="Edit card"
            SubmitCommand="{Binding EditCardCommand}"
            CloseButtonText="Cancel"
            CloseCommand="{Binding CloseCommand}"
            Front="{Binding Front, UpdateSourceTrigger=PropertyChanged}"
            Back="{Binding Back, UpdateSourceTrigger=PropertyChanged}"
            CanSubmit="{Binding CanEditCard}"/>
    </Grid>
</UserControl>
