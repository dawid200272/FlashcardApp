<UserControl x:Class="FlashcardApp.WPF.Views.ChangeDeckNameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlashcardApp.WPF.Views"
             
             xmlns:viewmodels="clr-namespace:FlashcardApp.WPF.ViewModels"
             xmlns:controls="clr-namespace:FlashcardApp.WPF.Controls"
             
             d:DataContext="{d:DesignInstance Type=viewmodels:ChangeDeckNameViewModel}"
             
             mc:Ignorable="d" 
             Width="500" Height="160">
    <UserControl.Resources>
        <Style TargetType="TextBlock" BasedOn="{StaticResource whiteFont}" />
    </UserControl.Resources>
    <Grid>
        <controls:DeckNameForm
            DeckName="{Binding NewDeckName}"
            CanSubmit="{Binding CanSubmit}"
            SubmitCommand="{Binding ChangeDeckNameCommand}"
            CloseCommand="{Binding CloseCommand}"/>
    </Grid>
</UserControl>
